# replace the double spase from problem 3 with single spaces.
name = "faiz  is a good boy"
print(name.replace("  "," "))
print(name) # string are immutable which means that you cannot change them by runnig functions on them